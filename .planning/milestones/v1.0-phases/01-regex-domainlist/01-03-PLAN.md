---
phase: 01-regex-domainlist
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - internal/dns/regex_integration_test.go
requirements:
  - REGEX-01
  - REGEX-02
  - REGEX-03
  - REGEX-04
  - REGEX-05
autonomous: true
must_haves:
  truths:
    - "User can add regex:([a-z]+)\\.internal\\.corp lines to domainlist and queries for foo.internal.corp resolve correctly"
    - "Exact domain entries still resolve with same behavior as before"
    - "Wildcard entries match subdomains"
    - "DNS query priority is exact > wildcard > regex > catch-all"
    - "Invalid regex patterns produce a warning and do not block loading"
  artifacts:
    - path: internal/dns/regex_integration_test.go
      provides: 7 integration tests covering end-to-end regex domainlist workflow
  key_links:
    - from: cache.load()
      to: mux.HandleRegex()
      via: regex: prefix detection in load()
    - from: mux.ServeDNS()
      to: c.resolve()
      via: priority-based handler dispatch
---

# Phase 01-03: Integration Tests

## Objective

Create end-to-end integration tests that verify the complete regex domainlist workflow: loading a file with mixed exact/wildcard/regex entries, confirming priority routing works correctly, and validating that invalid regex patterns are gracefully skipped.

Purpose: Prove that all 5 requirements (REGEX-01 through REGEX-05) are satisfied by the integrated system. These tests exercise the full path from file I/O through mux routing to handler dispatch.

Output: `internal/dns/regex_integration_test.go` with 7 integration tests.

## Execution Context

@/home/agent/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/agent/.config/opencode/gsd-core/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/phases/01-regex-domainlist/01-CONTEXT.md
@.planning/phases/01-regex-domainlist/01-RESEARCH.md
@.planning/phases/01-regex-domainlist/01-01-PLAN.md
@.planning/phases/01-regex-domainlist/01-02-PLAN.md
internal/dns/serveMux.go
internal/dns/serveMux_test.go
internal/dns/cache.go
internal/dns/cache_test.go
internal/dns/main.go

## Tasks

<task type="auto">
  <id>01-03.1</id>
  <name>Create integration tests for regex domainlist loading and routing</name>
  <wave>3</wave>
  <depends_on>
    <dep>01-02</dep>
  </depends_on>
  <read_first>
    <path>internal/dns/serveMux.go</path>
    <purpose>Verify regexServeMux method signatures and internal field names for test access</purpose>
  </read_first>
  <read_first>
    <path>internal/dns/cache.go</path>
    <purpose>Verify register(), registerRegex(), load() signatures and behavior after Wave 1 changes</purpose>
  </read_first>
  <read_first>
    <path>internal/dns/cache_test.go</path>
    <purpose>Reference for test patterns, newTestCache usage, temp file creation</purpose>
  </read_first>
  <read_first>
    <path>internal/dns/serveMux_test.go</path>
    <purpose>testResponseWriter mock for DNS query simulation — already defined in serveMux_test.go, shared across test files in same package</purpose>
  </read_first>
  <acceptance_criteria>
    - [ ] internal/dns/regex_integration_test.go exists with package dns
    - [ ] 7 test functions: LoadRegexPattern, RegexMatchInMux, InvalidRegexSkippedWithWarning, PriorityExactOverRegex, WildcardInDomainlist, LoadClearsPreviousState, ExactMatchStillWorks
    - [ ] Each test uses newTestCache(t) or newRegexServeMux() for setup
    - [ ] Tests use os.WriteFile + filepath.Join for temp domainlist files
    - [ ] Tests use github.com/stretchr/testify/require for setup assertions
    - [ ] Tests use github.com/stretchr/testify/assert for behavior assertions
    - [ ] Tests verify actual handler behavior (not just generation count)
    - [ ] go test ./internal/dns/ -run TestRegexIntegration -v -count=1 passes all 7 tests
  </acceptance_criteria>
  <action>
Create internal/dns/regex_integration_test.go with 7 integration tests.

Shared test helper (DO NOT define — already in serveMux_test.go, same package dns):
- testResponseWriter is defined in serveMux_test.go. Reference it directly.
INFO 2 Fix: Removed duplicate testResponseWriter definition from this file. Both test files are in package dns, so the helper is shared.

Test functions (7 total):

1. TestRegexIntegration_LoadRegexPattern
   - c := newTestCache(t)
   - tmpDir := t.TempDir()
   - listFile := filepath.Join(tmpDir, "test.lst")
   - content := "example.com\nregex:([a-z]+)\\.internal\\.corp\n"
   - os.WriteFile(listFile, []byte(content), 0644)
   - err := c.load(listFile)
   - require.NoError(t, err)
   - assert.True(t, c.generation() > 0, "generation should increase after load")
   BLOCKER 2 Fix: Add behavioral assertions:
   - Verify regex pattern is registered by querying the mux:
     msg := new(dns.Msg)
     msg.SetQuestion("foo.internal.corp.", dns.TypeA)
     w := &testResponseWriter{}
     c.mux.ServeDNS(w, msg)
     assert.True(t, w.called, "regex-matched domain should trigger handler")

2. TestRegexIntegration_RegexMatchInMux
   - mux := newRegexServeMux()
   - called := false
   - mux.HandleRegex("([a-z]+)\\.internal\\.corp", func(w dns.ResponseWriter, r *dns.Msg) { called = true })
   - msg := new(dns.Msg)
   - msg.SetQuestion("foo.internal.corp.", dns.TypeA)
   - w := &testResponseWriter{}
   - mux.ServeDNS(w, msg)
   - assert.True(t, called, "regex handler should be called for foo.internal.corp")

3. TestRegexIntegration_InvalidRegexSkippedWithWarning
   - c := newTestCache(t)
   - tmpDir := t.TempDir()
   - listFile := filepath.Join(tmpDir, "invalid.lst")
   - content := "example.com\nregex:[invalid\ntest.com\n"
   - os.WriteFile(listFile, []byte(content), 0644)
   - err := c.load(listFile)
   - require.NoError(t, err, "load should succeed even with invalid regex")
   BLOCKER 2 Fix: Add behavioral assertions:
   - Verify example.com and test.com are registered (exact matches in mux):
     msg := new(dns.Msg)
     msg.SetQuestion("example.com.", dns.TypeA)
     w := &testResponseWriter{}
     c.mux.ServeDNS(w, msg)
     assert.True(t, w.called, "example.com should match registered handler")
   - Verify test.com is also registered:
     msg2 := new(dns.Msg)
     msg2.SetQuestion("test.com.", dns.TypeA)
     w2 := &testResponseWriter{}
     c.mux.ServeDNS(w2, msg2)
     assert.True(t, w2.called, "test.com should match registered handler")

4. TestRegexIntegration_PriorityExactOverRegex
   - mux := newRegexServeMux()
   - exactCalled := false; regexCalled := false
   - mux.HandleFunc("example.com", func(w dns.ResponseWriter, r *dns.Msg) { exactCalled = true })
   - mux.HandleRegex("ex.*", func(w dns.ResponseWriter, r *dns.Msg) { regexCalled = true })
   - msg := new(dns.Msg)
   - msg.SetQuestion("example.com.", dns.TypeA)
   - w := &testResponseWriter{}
   - mux.ServeDNS(w, msg)
   - assert.True(t, exactCalled, "exact match should take priority over regex")
   - assert.False(t, regexCalled, "regex handler should NOT be called when exact matches")

5. TestRegexIntegration_WildcardInDomainlist
   - c := newTestCache(t)
   - tmpDir := t.TempDir()
   - listFile := filepath.Join(tmpDir, "wildcard.lst")
   - content := "*.example.com\nregex:([a-z]+)\\.test\\.com\n"
   - os.WriteFile(listFile, []byte(content), 0644)
   - err := c.load(listFile)
   - require.NoError(t, err)
   - assert.True(t, c.generation() > 0)
   BLOCKER 2 Fix: Add behavioral assertion:
   - Verify wildcard *.example.com is registered by sending query for foo.example.com:
     msg := new(dns.Msg)
     msg.SetQuestion("foo.example.com.", dns.TypeA)
     w := &testResponseWriter{}
     c.mux.ServeDNS(w, msg)
     assert.True(t, w.called, "wildcard-matched domain should trigger handler")

6. TestRegexIntegration_LoadClearsPreviousState
   - c := newTestCache(t)
   - tmpDir := t.TempDir()
   - listFile1 := filepath.Join(tmpDir, "list1.lst")
   - listFile2 := filepath.Join(tmpDir, "list2.lst")
   - os.WriteFile(listFile1, []byte("example.com\n"), 0644)
   - os.WriteFile(listFile2, []byte("test.com\n"), 0644)
   - c.load(listFile1)
   BLOCKER 2 Fix: Add behavioral assertions:
   - Verify example.com IS registered before second load:
     msg := new(dns.Msg)
     msg.SetQuestion("example.com.", dns.TypeA)
     w := &testResponseWriter{}
     c.mux.ServeDNS(w, msg)
     assert.True(t, w.called, "example.com should match before reload")
   - Load second list (should clear old handlers):
     c.load(listFile2)
   - Verify example.com is NOT registered after reload (should fall through to catch-all):
     w2 := &testResponseWriter{}
     c.mux.ServeDNS(w2, msg)
     assert.False(t, w2.called, "example.com should NOT match after reload clears state")
   - Verify test.com IS registered after reload:
     msg2 := new(dns.Msg)
     msg2.SetQuestion("test.com.", dns.TypeA)
     w3 := &testResponseWriter{}
     c.mux.ServeDNS(w3, msg2)
     assert.True(t, w3.called, "test.com should match after reload")

7. TestRegexIntegration_ExactMatchStillWorks
   - c := newTestCache(t)
   - tmpDir := t.TempDir()
   - listFile := filepath.Join(tmpDir, "exact.lst")
   - content := "cloudflare.com\ngoogle.com\n"
   - os.WriteFile(listFile, []byte(content), 0644)
   - err := c.load(listFile)
   - require.NoError(t, err)
   - assert.True(t, c.generation() > 0)
   BLOCKER 2 Fix: Add behavioral assertions:
   - Verify cloudflare.com matches:
     msg := new(dns.Msg)
     msg.SetQuestion("cloudflare.com.", dns.TypeA)
     w := &testResponseWriter{}
     c.mux.ServeDNS(w, msg)
     assert.True(t, w.called, "cloudflare.com should match registered handler")
   - Verify google.com matches:
     msg2 := new(dns.Msg)
     msg2.SetQuestion("google.com.", dns.TypeA)
     w2 := &testResponseWriter{}
     c.mux.ServeDNS(w2, msg2)
     assert.True(t, w2.called, "google.com should match registered handler")

Import requirements:
- "os"
- "path/filepath"
- "testing"
- "net"
- "github.com/miekg/dns"
- "github.com/stretchr/testify/assert"
- "github.com/stretchr/testify/require"
  <verify>

    <automated>go test ./internal/dns/ -run TestRegexIntegration -v -count=1</automated>

  </verify>

  <done>
- internal/dns/regex_integration_test.go exists with 7 integration test functions
- All tests pass: LoadRegexPattern, RegexMatchInMux, InvalidRegexSkippedWithWarning, PriorityExactOverRegex, WildcardInDomainlist, LoadClearsPreviousState, ExactMatchStillWorks
- Each test includes behavioral assertions (not just generation count checks)
- go test ./internal/dns/ -run TestRegexIntegration -v -count=1 returns PASS for all 7 tests
  </done>
</task>

<task type="auto">
  <id>01-03.2</id>
  <name>Full test suite verification — all 25 tests pass</name>
  <wave>3</wave>
  <depends_on>
    <dep>01-03.1</dep>
  </depends_on>
  <read_first>
    <path>internal/dns/serveMux_test.go</path>
    <purpose>Verify all 12 mux tests are present and passing</purpose>
  </read_first>
  <read_first>
    <path>internal/dns/cache_test.go</path>
    <purpose>Verify updated newTestCache with mux injection</purpose>
  </read_first>
  <read_first>
    <path>internal/dns/regex_integration_test.go</path>
    <purpose>Verify all 7 integration tests are present and passing</purpose>
  </read_first>
  <acceptance_criteria>
    - [ ] go test ./internal/dns/ -v -count=1 runs all 25 tests (6 cache + 12 mux + 7 integration)
    - [ ] All 25 tests PASS
    - [ ] go build ./... succeeds with no errors in any package
    - [ ] No test failures, panics, or race conditions
  </acceptance_criteria>
  <action>
Run the full test suite for the dns package and verify all tests pass:

1. go test ./internal/dns/ -v -count=1
   - Expected: 25 tests total (6 cache + 12 mux + 7 integration)
   - All must PASS
   - Count PASS lines: grep -c "^--- PASS" output

2. go build ./...
   - Must succeed with zero errors in all packages

3. go test ./internal/dns/ -race -count=1
   - Must pass with no data races detected
   - The regexServeMux uses sync.RWMutex correctly (RLock in ServeDNS, Lock in mutations)

INFO 1 Fix: Test count is 25 (6 cache + 12 mux + 7 integration). The checker incorrectly claimed Task 3 adds a test — it only removes a redundant SetMux call.
  <verify>

    <automated>go test ./internal/dns/ -v -count=1 && go test ./internal/dns/ -race -count=1</automated>

  </verify>

  <done>
- All 25 tests pass: 6 cache tests, 12 serveMux tests, 7 integration tests
- go build ./... succeeds with no errors
- go test -race passes with no data races
- Test output shows all tests PASS
  </done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domainlist file → regexServeMux | Untrusted regex patterns enter the system from operator-controlled file |
| DNS query → ServeDNS | External DNS clients send queries that traverse priority routing |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | E (Elevation) | regexServeMux.HandleRegex | mitigate | Patterns validated at load time via regexp.Compile() — invalid patterns return error, never compiled at runtime |
| T-01-02 | A (Availability) | regexServeMux.ServeDNS | accept | ReDoS risk mitigated by trusting operator-controlled domainlist file; daemon runs as root on controlled network per D-03 |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |

## Verification

- go test ./internal/dns/ -v -count=1 — all 25 tests PASS
- go build ./... — clean build
- go test ./internal/dns/ -race -count=1 — no data races

## Success Criteria

1. 7 integration tests cover: regex loading, regex matching, invalid regex skip, priority ordering, wildcard support, state clearing on reload, exact-match compatibility
2. All 25 tests pass (6 cache + 12 mux + 7 integration)
3. go build ./... succeeds
4. No data races detected with -race flag
5. Integration tests verify actual handler behavior (not just generation count)

## Output

Create `.planning/phases/01-regex-domainlist/01-03-SUMMARY.md` when done
