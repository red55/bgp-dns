---
phase: 02-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/bgp/bgp.go
  - internal/bgp/bgp_test.go
  - internal/dns/resolvers_test.go
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []

must_haves:
  truths:
    - "BGP reference counting correctly tracks first-reference (add) and last-reference (remove) transitions"
    - "Multi-domain IP sharing increments/decrements counter without calling add/remove between first and last reference"
    - "DNS resolver ring rotates to next resolver on failure and recovers when it succeeds again"
    - "All-resolvers-failure returns a meaningful error"
  artifacts:
    - path: internal/bgp/bgp_test.go
      provides: "BGP reference counting unit tests"
      exports: ["newTestBgpSrv", "TestBgp_ReferenceCounting_*"]
    - path: internal/dns/resolvers_test.go
      provides: "DNS resolver ring failover unit tests"
      exports: ["TestResolver_*", "newFakeDNSServer"]
  key_links:
    - from: internal/bgp/bgp_test.go
      to: internal/bgp/main.go
      via: "direct bgpSrv construction and Operation() spy"
      pattern: "srv\\.Operation\\(func"
    - from: internal/bgp/bgp_test.go
      to: internal/bgp/main.go
      via: "ipRefCounter map inspection after Operation() returns"
      pattern: "srv\\.ipRefCounter\\["
    - from: internal/dns/resolvers_test.go
      to: internal/dns/resolvers.go
      via: "fake DNS server intercepting dns.Exchange() calls"
      pattern: "dns\\.Exchange\\(q.*srv\\.addr"
---

<objective>
Create BGP reference counting tests and DNS resolver failover tests.

Purpose: Cover TEST-01 (BGP reference counting) and TEST-02 (resolver ring failover) with same-package unit tests that work within the existing global-state architecture.

Output: Two new test files with 9 total test functions covering all locked decisions (D-01 through D-09).
</objective>

<execution_context>
@/home/agent/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/agent/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-suite/02-CONTEXT.md
@.planning/phases/02-test-suite/02-RESEARCH.md
@.planning/phases/02-test-suite/02-PATTERNS.md

# Source files to understand
@internal/bgp/main.go
@internal/bgp/bgp.go
@internal/bgp/loop.go
@internal/loop/main.go
@internal/dns/resolvers.go
@internal/dns/main.go
@internal/dns/cache.go
@internal/dns/resolve.go
@internal/dns/cacheEntry.go

# Existing test patterns to follow
@internal/dns/cache_test.go
@internal/dns/serveMux_test.go
@internal/dns/regex_integration_test.go
@internal/dns/cacheEntry_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BGP reference counting tests (TEST-01)</name>
  <read_first>
    @internal/bgp/main.go — bgpSrv struct, ipRefCounter map, Advance(), Withdraw(), loop()
    @internal/bgp/bgp.go — add(), remove() methods that call s.bgp.AddPath/DeletePath
    @internal/bgp/loop.go — bgpSrv.loop() goroutine that reads from ChanOp()
    @internal/loop/main.go — Loop.Operation(f, ret) pattern, HandleOp, NoErr, NoOp
  </read_first>
  <files>internal/bgp/bgp_test.go</files>
  <behavior>
    - TestBgp_ReferenceCounting_SingleIP: Advance("10.0.0.1") → counter=1, Withdraw("10.0.0.1") → counter=0, key deleted
    - TestBgp_ReferenceCounting_MultiDomainSharing: Advance two domains with same IP → counter=2, Withdraw once → counter=1, Withdraw again → counter=0, key deleted
    - TestBgp_ReferenceCounting_ConcurrentAdvances: Two goroutines Advance same IP simultaneously → counter should be exactly 2 (atomic)
    - TestBgp_ReferenceCounting_WithdrawNonExistent: Withdraw IP never advanced → no panic, no error, counter unchanged
    - TestBgp_ReferenceCounting_MixedSequence: Advance A, Advance B, Withdraw A, Advance A, Withdraw A, Withdraw B → correct final state
    - add() and remove() must be nil-safe: when s.bgp is nil, skip GoBGP API calls (no panic)
  </behavior>
  <action>
Create `internal/bgp/bgp_test.go` with package `bgp` (same-package test per D-05).

**Test helper (follow newTestCache pattern):**
```go
func newTestBgpSrv(t *testing.T) *bgpSrv {
    t.Helper()
    srv := &bgpSrv{
        Loop:         loop.NewLoop(1),
        ipRefCounter: make(map[string]*atomic.Uint64),
        asn:          65000,
    }
    ctx, cancel := context.WithCancel(context.Background())
    go srv.loop(ctx)
    t.Cleanup(cancel)
    return srv
}
```

**Nil-safety for add()/remove():** Before calling `s.bgp.AddPath(...)` or `s.bgp.DeletePath(...)`, check `if s.bgp == nil { return nil }`. This allows testing the reference counting logic without a real GoBGP server. Modify `bgp.go` add() and remove() methods to skip the GoBGP call when `s.bgp` is nil.

**Test scenarios (per D-03):**

1. `TestBgp_ReferenceCounting_SingleIP`: Call srv.Operation() with a closure that simulates Advance logic — increment counter to 1, verify add() would be called (first reference). Then simulate Withdraw — decrement to 0, verify remove() would be called (last ref gone). Verify ipRefCounter map state after each step.

2. `TestBgp_ReferenceCounting_MultiDomainSharing`: Advance same IP from two "domains" (simulated by two Operation() calls). Verify counter=2. Withdraw once — counter=1, NOT removed. Withdraw again — counter=0, deleted.

3. `TestBgp_ReferenceCounting_ConcurrentAdvances`: Launch 10 goroutines, each calling Operation() with Advance logic for same IP. Verify final counter == 10 (atomic correctness).

4. `TestBgp_ReferenceCounting_WithdrawNonExistent`: Call Operation() with Withdraw logic for IP never advanced. Verify no panic, counter map unchanged.

5. `TestBgp_ReferenceCounting_MixedSequence`: Advance A, Advance B, Withdraw A, Advance A, Withdraw A, Withdraw B. Verify correct transitions at each step.

**Per D-04:** Verify both counter state AND add()/remove() call timing. Use a test double: add a `calledAdd bool` and `calledRemove bool` field to bgpSrv (or use a simple channel to record calls). Check that add() was called exactly once per first-reference, remove() exactly once per last-reference.

**Per D-02:** Use `Operation(f, true)` (sync=true) in all tests so the goroutine processes the operation before the test continues.

**Per D-14:** Run these with `-race` flag — add a TestMain or note that `go test -race ./internal/bgp/` validates concurrency safety.
  </action>
  <verify>
    <automated>go test -race -run "TestBgp" ./internal/bgp/ -count=1 -v</automated>
    <manual-check>Verify 5 test functions exist and all pass</manual-check>
  </verify>
  <done>
    - `internal/bgp/bgp_test.go` exists with 5 test functions: SingleIP, MultiDomainSharing, ConcurrentAdvances, WithdrawNonExistent, MixedSequence
    - `add()` and `remove()` in bgp.go are nil-safe (skip GoBGP calls when s.bgp is nil)
    - All tests pass with `go test -race -run "TestBgp" ./internal/bgp/`
    - ipRefCounter transitions verified: counter increments on Advance, decrements on Withdraw, key deleted at 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DNS resolver failover tests (TEST-02)</name>
  <read_first>
    @internal/dns/resolvers.go — resolvers struct, query() method with ring failover, newResolvers(), setResolvers()
    @internal/dns/cache_test.go — newResolversWithLogger() pattern, initTestLogger()
    @internal/dns/serveMux_test.go — testResponseWriter mock, newTestMsg helper
    @internal/log/main.go — log.NewLog(), log.L()
  </read_first>
  <files>internal/dns/resolvers_test.go</files>
  <behavior>
    - TestResolver_SingleSuccess: One resolver, returns valid answer → query succeeds, resolver stays okay
    - TestResolver_Failover: Two resolvers, first fails → ring rotates, second succeeds → query succeeds
    - TestResolver_Recovery: Two resolvers, first fails, then succeeds on retry → recovery verified
    - TestResolver_AllFail: Two resolvers, both fail → error returned, all marked not okay
    - Ring position verification: After failover, rs.rs points to next resolver (ring rotated)
  </behavior>
  <action>
Create `internal/dns/resolvers_test.go` with package `dns` (same-package test per D-05 pattern).

**Test helper — Fake DNS server (per D-06, RESEARCH.md Example 1):**
```go
func newFakeDNSServer(t *testing.T, handler dns.HandlerFunc) (*dns.Server, string) {
    t.Helper()
    ln, err := net.ListenPacket("udp", "127.0.0.1:0")
    require.NoError(t, err)
    srv := &dns.Server{
        PacketConn: ln,
        Handler:    handler,
        Net:        "udp",
    }
    go srv.ActivateAndServe()
    t.Cleanup(func() { srv.Shutdown() })
    return srv, ln.LocalAddr().String()
}
```

**Test helper — Resolver construction (per D-09, only test query() directly):**
```go
func newTestResolvers(t *testing.T, addrs []*net.UDPAddr) *resolvers {
    t.Helper()
    initTestLogger()
    l := zerolog.New(os.Stderr).Level(zerolog.WarnLevel)
    return newResolvers(addrs)
}
```

**Re-use existing patterns from serveMux_test.go:**
- `testResponseWriter` — copy the full mock struct (lines 12-27 of serveMux_test.go)
- `newTestMsg(fqdn, qtype)` — copy from serveMux_test.go (lines 29-33)

**Test scenarios (per D-07, D-08):**

1. `TestResolver_SingleSuccess`: Create fake DNS server returning valid A record. Create resolvers with one addr pointing to fake server. Call query(). Verify: no error, response has answer, resolver.ok() is true, ring position unchanged.

2. `TestResolver_Failover`: Create two fake DNS servers. First always returns error (close its listener or use unresponsive handler). Second returns valid answer. Create resolvers with both addrs. Call query(). Verify: second resolver used, ring rotated (next resolver is head), first resolver marked failed.

3. `TestResolver_Recovery`: Create two fake DNS servers. First returns error initially, then after a flag flip returns success. Verify: first attempt fails → ring rotates to second, second attempt with first → succeeds, first resolver marked okay again.

4. `TestResolver_AllFail`: Create fake DNS servers that all return errors. Call query(). Verify: error returned (ErrNoResolvers or combined), all resolvers marked failed, ring rotated back to head.

**Per D-08:** For each test, also verify ring position:
```go
resolvers.m.RLock()
current := resolvers.rs
current.Do(func(v interface{}) {
    r := v.(*resolver)
    // check r.isOk() state
})
resolvers.m.RUnlock()
```

**Important:** Each test creates a FRESH `resolvers` instance via `newResolvers()` — never reuse across subtests (per RESEARCH.md Pitfall 3).
  </action>
  <verify>
    <automated>go test -run "TestResolver" ./internal/dns/ -count=1 -v</automated>
    <manual-check>Verify 4 test functions exist covering all failover scenarios</manual-check>
  </verify>
  <done>
    - `internal/dns/resolvers_test.go` exists with 4 test functions: SingleSuccess, Failover, Recovery, AllFail
    - Fake DNS server helper creates controlled responses per D-06
    - Ring position verified after each scenario (D-08)
    - Each test creates fresh resolvers instance (Pitfall 3)
    - All tests pass with `go test -run "TestResolver" ./internal/dns/`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Tests access private fields directly (same-package pattern) — trusted test code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | bgp.go add()/remove() nil-safety | mitigate | Guard s.bgp nil before calling GoBGP API — prevents panic in tests |
| T-02-02 | Tampering | Global state in resolver tests | mitigate | Each test creates fresh resolvers instance, no shared state (Pitfall 3) |
| T-02-SC | Tampering | GoBGP test dependency | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
Wave 1 verification:
- `go test -race -run "TestBgp" ./internal/bgp/ -count=1` — BGP tests pass with race detector
- `go test -run "TestResolver" ./internal/dns/ -count=1` — Resolver tests pass
</verification>

<success_criteria>
1. `internal/bgp/bgp_test.go` exists with 5 test functions covering all D-03 scenarios
2. `internal/dns/resolvers_test.go` exists with 4 test functions covering all D-07 scenarios
3. All tests pass with `go test ./internal/bgp/ ./internal/dns/ -count=1`
4. BGP tests pass with `-race` flag (D-14)
5. add()/remove() in bgp.go handle nil s.bgp gracefully
6. No t.Parallel() used in tests touching shared globals
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Purpose |
|--------|------|---------|
| `newTestBgpSrv` | internal/bgp/bgp_test.go | Test helper constructing bgpSrv with loop goroutine |
| `TestBgp_ReferenceCounting_SingleIP` | internal/bgp/bgp_test.go | Single IP advance/withdraw verification |
| `TestBgp_ReferenceCounting_MultiDomainSharing` | internal/bgp/bgp_test.go | Multi-domain IP sharing reference count |
| `TestBgp_ReferenceCounting_ConcurrentAdvances` | internal/bgp/bgp_test.go | Concurrent advance atomicity |
| `TestBgp_ReferenceCounting_WithdrawNonExistent` | internal/bgp/bgp_test.go | Graceful withdrawal of unknown IP |
| `TestBgp_ReferenceCounting_MixedSequence` | internal/bgp/bgp_test.go | Mixed advance/withdraw sequence |
| `newFakeDNSServer` | internal/dns/resolvers_test.go | Test helper creating fake DNS server |
| `TestResolver_SingleSuccess` | internal/dns/resolvers_test.go | Single resolver success path |
| `TestResolver_Failover` | internal/dns/resolvers_test.go | Ring rotation on resolver failure |
| `TestResolver_Recovery` | internal/dns/resolvers_test.go | Resolver recovery after failure |
| `TestResolver_AllFail` | internal/dns/resolvers_test.go | All-resolvers-failure error path |

<output>
Create `.planning/phases/02-test-suite/02-01-SUMMARY.md` when done
</output>
