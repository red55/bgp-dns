---
context: phase
phase: 04-reliability
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-06-16T15:50:06.130Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_None yet — Phase 4 has not been planned._

## Critical Anti-Patterns

_None recorded._

<current_state>
Phase 3 (Dependency Injection) is COMPLETE. All 6 plans executed, committed, and handoff artifacts removed.

Phase 4 (Reliability) is NOT STARTED. The directory `.planning/phases/04-reliability/` exists but is empty.

Gray areas discussed and resolved in conversation (not yet committed to Phase 4 artifacts):
- Gray Area 1: DNS timeout configuration — resolved (single global Dns.Timeout, default 5s, range 3s–30s, per-resolver cached dns.Client)
- Gray Area 2: BGP context propagation — resolved (store daemon lifecycle context in bgpSrv struct, pass to all GoBGP API calls)

Gray areas NOT yet discussed:
- Gray Area 3: BGP error logging — Warn vs Error? What context?
- Gray Area 4: O(n) set difference — Reuse CONCERNS.md suggestion or inline in cache.go?

No uncommitted working directory changes. Ready to continue Phase 4 discussion or start planning.
</current_state>

<completed_work>

Phase 3 — All 6 waves complete:
- Wave 0 (03-01): Foundation — typed configKey, Loop/logger injection, cache config
- Wave 1 (03-02): DNS Service struct + constructors + wrappers
- Wave 2 (03-03): BGP Service struct + constructors + wrappers
- Wave 3 (03-04): FSWatcher Service struct + constructors + wrappers
- Wave 4 (03-05): main.go panic-to-error migration
- Wave 5 (03-06): Dead code removal

All 63 tests pass with -race detector.

Phase 4 — Gray area discussion started:
- RELIAB-01 (DNS timeout): resolved design decisions
- RELIAB-03 (BGP context): resolved design decisions
- RELIAB-02 (O(n) set difference): not discussed
- RELIAB-04 (BGP error logging): not discussed
</completed_work>

<remaining_work>

Phase 4: Reliability — PLANNING PHASE

Remaining gray areas to discuss:
- Gray Area 3: BGP error logging — severity (Warn vs Error), context (peer IP, operation, IPs affected)
- Gray Area 4: O(n) set difference — where to implement (inline in cache.go vs utility function in utils/main.go)

After all gray areas resolved:
- Write 04-CONTEXT.md with locked decisions
- Create 04-PLAN.md with waves
- Execute Phase 4 plans

Phase 5: Security — NOT STARTED
</remaining_work>

<decisions_made>

DNS timeout (RELIAB-01) — resolved:
- Single global `Dns.Timeout` in config (Option A)
- Default 5 seconds, range 3s–30s
- Per-resolver cached `*dns.Client` (not per-query)
- Implementation: add `Timeout time.Duration` to `dnsCfg`, validate in config init, store in resolvers struct

BGP context propagation (RELIAB-03) — resolved:
- Store daemon lifecycle context in `bgpSrv.ctx` field
- `NewBgp(ctx, cfg, loop, logger)` — receives context from main.go
- `add/remove/find` methods use `s.ctx` instead of `context.Background()`
- On shutdown, `s.cancel()` cancels the context → loop exits → in-flight BGP calls cancelled
- Test helper `NewBgpSrvForTest` should also accept context parameter
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. .planning/ROADMAP.md — Phase 4 goal, success criteria, dependencies
2. .planning/REQUIREMENTS.md — RELIAB-01 through RELIAB-04
3. .planning/PROJECT.md — Active requirements (RELIAB items)
4. .planning/phases/02-test-suite/02-VERIFICATION.md — Tests to preserve during Phase 4
5. .planning/codebase/CONCERNS.md — O(n) set difference suggestion
6. internal/dns/resolvers.go — Current dns.Exchange() usage (no timeout)
7. internal/bgp/bgp.go — Current context.Background() usage in add/remove/find
8. internal/bgp/main.go — bgpSrv struct with cancel field
9. cmd/bgp-dnsd/main.go — Daemon lifecycle context creation

## Critical Anti-Patterns (do NOT repeat these)
- Do NOT remove global state before tests exist — Phase 2 tests are the safety net
- Do NOT parallelize tests that touch shared globals — use sequential test execution
- Do NOT change behavior — Phase 4 is reliability improvements, not feature changes
- Ensure Phase 4 tests pass before AND after changes (Phase 2 suite is the regression check)

## Infrastructure State
- No running servers or background processes
- All tests pass in clean state
- Working directory is clean (no uncommitted changes)

## Pre-Execution Critique Required
<!-- None — Phase 4 has not been planned yet -->

<context>
Project is in a clean state after completing Phase 3. Phase 4 (Reliability) is the next planned phase.

Phase 4 goal: Fix known reliability issues: DNS timeout, O(n²) set difference, BGP context propagation, error handling.

Requirements: RELIAB-01 (DNS timeout configurable via YAML, default 5s), RELIAB-02 (set difference O(n) via maps), RELIAB-03 (BGP ops use cancellable context), RELIAB-04 (BGP Advance/Withdraw errors logged at Warn level).

Gray areas 1 and 2 have been discussed and resolved in conversation. Gray areas 3 and 4 need discussion before planning.

Phase 4 depends on Phase 3 being complete (it is) and Phase 2 tests passing (they do).
</context>

<next_action>
Continue Phase 4 gray area discussion:
- Gray Area 3: BGP error logging (severity, context)
- Gray Area 4: O(n) set difference (implementation location)

After all gray areas resolved, create 04-CONTEXT.md and plan Phase 4.
</next_action>
