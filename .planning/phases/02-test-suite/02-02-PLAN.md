---
phase: 02-test-suite
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - internal/dns/cache_eviction_test.go
  - cmd/bgp-dnsd/cli/grpc_lifecycle_test.go
autonomous: true
requirements:
  - TEST-03
  - TEST-04
user_setup: []

must_haves:
  truths:
    - "Cache entries from old generation are removed when a new domainlist file is loaded"
    - "Cache entries expire after their TTL duration"
    - "Cache respects maximum entry capacity via LFU eviction"
    - "gRPC server starts on a port, accepts RPCs, and shuts down gracefully"
  artifacts:
    - path: internal/dns/cache_eviction_test.go
      provides: "Cache eviction tests (TTL + generation-based)"
      exports: ["TestCache_Eviction_*"]
    - path: cmd/bgp-dnsd/cli/grpc_lifecycle_test.go
      provides: "gRPC server lifecycle integration tests"
      exports: ["TestGRPC_*"]
  key_links:
    - from: internal/dns/cache_eviction_test.go
      to: internal/dns/cache.go
      via: "load() triggers evictByGeneration() which unregisters old entries"
      pattern: "c\\.load\\("
    - from: cmd/bgp-dnsd/cli/grpc_lifecycle_test.go
      to: cmd/bgp-dnsd/cli/cache.go
      via: "net.Listen ephemeral port + grpc.NewServer + RegisterBgpDnsServiceServer"
      pattern: "net\\.Listen\\(\"tcp\".*:0"
---

<objective>
Create cache eviction tests and gRPC CLI lifecycle integration tests.

Purpose: Cover TEST-03 (cache eviction on generation change) and TEST-04 (gRPC server lifecycle). These tests verify observable behaviors through exported methods since internal fields are not directly accessible from test files in different packages.

Output: Two new test files with 5 total test functions.
</objective>

<execution_context>
@/home/agent/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/agent/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-test-suite/02-CONTEXT.md
@.planning/phases/02-test-suite/02-RESEARCH.md
@.planning/phases/02-test-suite/02-PATTERNS.md

# Source files to understand
@internal/dns/cache.go
@internal/dns/cacheEntry.go
@internal/dns/main.go
@internal/dns/cache_test.go
@cmd/bgp-dnsd/cli/cache.go
@cmd/bgp-dnsd/cli/cache_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache eviction tests (TEST-03)</name>
  <read_first>
    @internal/dns/cache.go — load(), evictByGeneration(), unregister(), upsert(), get(), findKeysByGeneration()
    @internal/dns/cache_test.go — newTestCache(), initTestLogger(), existing test patterns
    @internal/dns/cacheEntry.go — cacheEntry struct, generation field, Ip4s()
    @internal/dns/main.go — Load(), DumpCache(), ClearCache() exported functions, _cache global
  </read_first>
  <files>internal/dns/cache_eviction_test.go</files>
  <behavior>
    - TestCache_GenerationEviction: Load file with domains → verify entries exist → load same file again → verify old entries evicted
    - TestCache_TTLExpiration: Load file → entries evicted after TTL duration (using time.Sleep)
    - TestCache_CapacityLimit: Load many domains → verify cache respects max entries (LFU eviction of least-used)
    - TestCache_EvictionTriggersWithdraw: When old-generation entry is evicted, bgp.Withdraw() is called for its IPs
    - TestCache_MultiDomainEviction: Load file with 3 domains, verify all 3 registered → load again → verify all 3 evicted
  </behavior>
  <action>
Create `internal/dns/cache_eviction_test.go` with package `dns` (same-package test).

**Note:** This test file extends `cache_test.go` which already has `newTestCache()` and `initTestLogger()` helpers. Put the new tests in this separate file for clarity.

**Test scenarios:**

1. `TestCache_GenerationEviction`:
   ```go
   func TestCache_GenerationEviction(t *testing.T) {
       c := newTestCache(t)
       
       tmpDir := t.TempDir()
       listFile := filepath.Join(tmpDir, "test.lst")
       err := os.WriteFile(listFile, []byte("example.com\n"), 0644)
       require.NoError(t, err)
       
       // Load first time
       err = c.load(listFile)
       require.NoError(t, err)
       gen1 := c.generation()
       assert.Equal(t, uint64(1), gen1)
       
       // Verify entry is registered in mux
       _, hasExample := c.mux.exact["example.com."]
       assert.True(t, hasExample, "example.com should be registered after first load")
       
       // Load again (increments generation)
       err = c.load(listFile)
       require.NoError(t, err)
       gen2 := c.generation()
       assert.Equal(t, uint64(2), gen2, "generation should increase on second load")
       
       // Old entry should be evicted (generation 1 entries removed)
       _, hasExampleAfter := c.mux.exact["example.com."]
       assert.False(t, hasExampleAfter, "example.com should be evicted after generation increase")
   }
   ```

2. `TestCache_TTLExpiration`:
   - Create cache with known minTtl (e.g., 1 second)
   - Load a domain, trigger a DNS resolution (via upsert)
   - Sleep for TTL + 100ms for deterministic testing (gcache FakeClock availability uncertain)
    - Verify entry is no longer in cache via `c.get()`

3. `TestCache_CapacityLimit`:
   - Create cache with small max entries (e.g., 3) via `newCache(3, ...)`
   - Load 5 domains
   - Verify only ~3 entries remain (LFU eviction)
   - Use `c.entries.GetALL(true)` to inspect count (same-package access)

4. `TestCache_EvictionTriggersWithdraw`:
   - This tests the `onEntryEvicted` callback (cache.go line 51)
   - Set up a mock bgp.Withdraw — since bgp.Withdraw calls `_bgp.Operation()`, and `_bgp` is nil in tests, we need to intercept this
   - Alternative: Verify that `onEntryEvicted` is called by checking the log output (capture zerolog output)
   - Best approach: Test that when an entry is evicted, the cache calls `bgp.Withdraw()` for its IPs. Since bgp.Withdraw operates on `_bgp` global, construct a minimal bgpSrv for the test.

5. `TestCache_MultiDomainEviction`:
   - Load file with 3 domains
   - Verify all 3 registered
   - Load again (generation increases)
   - Verify all 3 evicted
   - Verify generation increased

**Per D-13:** Use embedded strings for test data via `t.TempDir()` + `os.WriteFile()`.

**Important:** Do NOT use `t.Parallel()` for tests modifying `_cache` global state. Each test creates its own cache instance via `newTestCache(t)`.
  </action>
  <verify>
    <automated>go test -run "TestCache_GenerationEviction|TestCache_TTL|TestCache_Capacity|TestCache_MultiDomain" ./internal/dns/ -count=1 -v</automated>
    <manual-check>Verify 5 eviction test functions exist and pass</manual-check>
  </verify>
  <done>
    - `internal/dns/cache_eviction_test.go` exists with 5 test functions
    - Generation-based eviction verified: old entries removed when generation increments
    - TTL expiration verified: entries evicted after TTL duration
    - Capacity limits verified: LFU eviction when max entries exceeded
    - All tests pass with `go test ./internal/dns/ -run "TestCache_"`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gRPC CLI lifecycle tests (TEST-04)</name>
  <read_first>
    @cmd/bgp-dnsd/cli/cache.go — CacheCliServiceImpl, ListCacheEntries, ClearCache, ReloadList, Serve(), Shutdown()
    @cmd/bgp-dnsd/cli/cache_test.go — existing nil request tests (2 tests)
    @internal/dns/main.go — Load(), DumpCache(), ClearCache() functions that _cache nil-check
  </read_first>
  <files>cmd/bgp-dnsd/cli/grpc_lifecycle_test.go</files>
  <behavior>
    - TestGRPC_ServeShutdown: Start server, connect client, call RPCs, shutdown — full lifecycle
    - TestGRPC_ListCacheEntries_Uninitialized: ListCacheEntries returns FailedPrecondition when cache not initialized
    - TestGRPC_ClearCache_Uninitialized: ClearCache returns FailedPrecondition when cache not initialized
    - TestGRPC_ReloadList_NilRequest: ReloadList returns InvalidArgument for nil request
    - TestGRPC_ReloadList_Uninitialized: ReloadList returns FailedPrecondition when cache not initialized
    - TestGRPC_GracefulStop: Server stops accepting new connections after GracefulStop()
  </behavior>
  <action>
Create `cmd/bgp-dnsd/cli/grpc_lifecycle_test.go` with package `cli` (same-package test).

**Test helper — gRPC server on ephemeral port (per RESEARCH.md Example 4):**
```go
func newTestGRPCLifecycle(t *testing.T) (*grpc.Server, string, func()) {
    t.Helper()
    listener, err := net.Listen("tcp", "127.0.0.1:0")
    require.NoError(t, err)
    
    grpcServer := grpc.NewServer()
    api.RegisterBgpDnsServiceServer(grpcServer, &CacheCliServiceImpl{})
    
    go grpcServer.Serve(listener)
    
    cleanup := func() {
        grpcServer.GracefulStop()
        listener.Close()
    }
    
    t.Cleanup(cleanup)
    return grpcServer, listener.Addr().String(), cleanup
}
```

**Test scenarios (per D-15):**

1. `TestGRPC_ServeShutdown`:
   - Start gRPC server on ephemeral port
   - Connect client via `grpc.Dial(addr, grpc.WithTransportCredentials(insecure.NewCredentials()))`
   - Call ListCacheEntries — expect FailedPrecondition (cache not initialized)
   - Call ClearCache with nil request — expect InvalidArgument
   - Call ReloadList with nil request — expect InvalidArgument
   - Call Shutdown() — verify server stops gracefully
   - Verify no panics or goroutine leaks

2. `TestGRPC_ListCacheEntries_Uninitialized`:
   - Start server, connect client
   - Call ListCacheEntries(context.Background(), &emptypb.Empty{})
   - Verify stream.Recv() returns FailedPrecondition (cache not initialized)

3. `TestGRPC_ClearCache_Uninitialized`:
   - Start server, connect client
   - Call ClearCache(context.Background(), &api.ClearCacheRequest{})
   - Verify error is FailedPrecondition

4. `TestGRPC_ReloadList_NilRequest`:
   - Start server, connect client
   - Call ReloadList(context.Background(), nil)
   - Verify error is InvalidArgument with message "request cannot be nil"

5. `TestGRPC_ReloadList_Uninitialized`:
   - Start server, connect client
   - Create temp file for list
   - Call ReloadList(context.Background(), &api.ReloadListRequest{ListFile: "/tmp/test.lst"})
   - Verify error is FailedPrecondition (cache not initialized)

6. `TestGRPC_GracefulStop`:
   - Start server, connect client
   - Call GracefulStop()
   - Verify new connections are rejected
   - Verify existing RPCs complete before stop

**Per RESEARCH.md Pitfall 4:** Use `net.Listen("tcp", "127.0.0.1:0")` — listener is ready immediately after Listen returns, no race condition.

**Important:** Use `t.Cleanup()` for goroutine/server teardown (not defer) to ensure cleanup runs even if test panics.

**Existing test compatibility:** The existing tests in `cache_test.go` test nil-request handling. This new file tests full lifecycle. Both files coexist in the same package.
  </action>
  <verify>
    <automated>go test -run "TestGRPC" ./cmd/bgp-dnsd/cli/ -count=1 -v</automated>
    <manual-check>Verify 6 lifecycle test functions exist and pass</manual-check>
  </verify>
  <done>
    - `cmd/bgp-dnsd/cli/grpc_lifecycle_test.go` exists with 6 test functions
    - Full lifecycle: Serve → RPCs → Shutdown works without errors
    - Error codes verified: InvalidArgument for nil requests, FailedPrecondition for uninitialized cache
    - GracefulStop verified: server stops accepting new connections
    - All tests pass with `go test ./cmd/bgp-dnsd/cli/ -run "TestGRPC"`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gRPC client → gRPC server | Untrusted input via RPC calls — validated by existing nil checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Cache eviction via generation | mitigate | Test verifies old entries actually removed from mux (not just generation incremented) |
| T-02-04 | Tampering | gRPC server on ephemeral port | accept | localhost-only server, no network exposure |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
Wave 2 verification:
- `go test -run "TestCache_" ./internal/dns/ -count=1` — cache eviction tests pass
- `go test -run "TestGRPC" ./cmd/bgp-dnsd/cli/ -count=1` — gRPC lifecycle tests pass
</verification>

<success_criteria>
1. `internal/dns/cache_eviction_test.go` exists with 5 test functions covering generation eviction, TTL, capacity
2. `cmd/bgp-dnsd/cli/grpc_lifecycle_test.go` exists with 6 test functions covering full lifecycle
3. All tests pass with `go test ./internal/dns/ ./cmd/bgp-dnsd/cli/ -count=1`
4. Generation-based eviction verified through mux registration state
5. gRPC error codes match expected values (InvalidArgument, FailedPrecondition)
6. No t.Parallel() used in tests touching shared globals
</success_criteria>

## Artifacts this phase produces

| Symbol | File | Purpose |
|--------|------|---------|
| `TestCache_GenerationEviction` | internal/dns/cache_eviction_test.go | Old-generation entries removed on reload |
| `TestCache_TTLExpiration` | internal/dns/cache_eviction_test.go | Entries evicted after TTL duration |
| `TestCache_CapacityLimit` | internal/dns/cache_eviction_test.go | LFU eviction when max entries exceeded |
| `TestCache_EvictionTriggersWithdraw` | internal/dns/cache_eviction_test.go | Evicted entries call bgp.Withdraw |
| `TestCache_MultiDomainEviction` | internal/dns/cache_eviction_test.go | All old entries evicted on generation increase |
| `newTestGRPCLifecycle` | cmd/bgp-dnsd/cli/grpc_lifecycle_test.go | Test helper for gRPC server lifecycle |
| `TestGRPC_ServeShutdown` | cmd/bgp-dnsd/cli/grpc_lifecycle_test.go | Full lifecycle: Serve → RPCs → Shutdown |
| `TestGRPC_ListCacheEntries_Uninitialized` | cmd/bgp-dnsd/cli/grpc_lifecycle_test.go | ListCacheEntries returns FailedPrecondition |
| `TestGRPC_ClearCache_Uninitialized` | cmd/bgp-dnsd/cli/grpc_lifecycle_test.go | ClearCache returns FailedPrecondition |
| `TestGRPC_ReloadList_NilRequest` | cmd/bgp-dnsd/cli/grpc_lifecycle_test.go | ReloadList returns InvalidArgument for nil |
| `TestGRPC_ReloadList_Uninitialized` | cmd/bgp-dnsd/cli/grpc_lifecycle_test.go | ReloadList returns FailedPrecondition |
| `TestGRPC_GracefulStop` | cmd/bgp-dnsd/cli/grpc_lifecycle_test.go | GracefulStop rejects new connections |

<output>
Create `.planning/phases/02-test-suite/02-02-SUMMARY.md` when done
</output>
