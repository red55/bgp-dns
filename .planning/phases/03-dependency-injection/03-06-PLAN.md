---
phase: 03-dependency-injection
plan: 06
type: execute
wave: 5
depends_on:
  - "03-05"
files_modified:
  - internal/dns/resolvers.go
  - internal/bgp/main.go
autonomous: true
requirements:
  - REFACTOR-04
user_setup: []

must_haves:
  truths:
    - "Commented hashmap line removed from bgp/main.go line 19"
    - "Commented hashmap initialization removed from bgp/main.go line 46"
    - "Commented error handling block removed from resolvers.go lines 127-148"
    - "No commented-out code blocks remain in the refactored files"
    - "All tests still pass after dead code removal"
  artifacts:
    - path: internal/bgp/main.go
      does_not_contain: "//ipRefCounter *hashmap.Map"
      does_not_contain: "//ipRefCounter: hashmap.New"
    - path: internal/dns/resolvers.go
      does_not_contain: "cause :="
      does_not_contain: "opError *net.OpError"
  key_links:
    - from: internal/bgp/main.go
      to: internal/bgp/main.go
      via: removal of commented hashmap field and initialization
    - from: internal/dns/resolvers.go
      to: internal/dns/resolvers.go
      via: removal of commented error handling block
---

<objective>
Remove commented-out dead code from resolvers.go (D-08) and bgp/main.go (D-09).

Purpose: Per D-08, remove commented error handling block in internal/dns/resolvers.go:127-148 (22 lines). Per D-09, remove commented hashmap in internal/bgp/main.go:19 and :46. These are remnants from previous development iterations and serve no purpose. Removing them improves code cleanliness and eliminates confusion about whether the code is intentionally disabled.

Output: Clean source files with no commented-out code blocks.
</objective>

<execution_context>
@/home/agent/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/agent/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dependency-injection/03-CONTEXT.md
@.planning/phases/03-dependency-injection/03-RESEARCH.md
@.planning/phases/03-dependency-injection/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove commented hashmap from bgp/main.go and commented error handling from resolvers.go</name>
  <files>internal/bgp/main.go, internal/dns/resolvers.go</files>
  <read_first>
    - internal/bgp/main.go (lines 19, 46 — commented hashmap)
    - internal/dns/resolvers.go (lines 127-148 — commented error handling block)
  </read_first>
  <acceptance_criteria>
    - internal/bgp/main.go line 19: `//ipRefCounter *hashmap.Map[string, *atomic.Uint64]` removed
    - internal/bgp/main.go line 46: `//ipRefCounter: hashmap.New[string, *atomic.Uint64](),` removed
    - internal/dns/resolvers.go lines 127-148: entire `/* ... */` block removed
    - `go build ./internal/bgp ./internal/dns` succeeds
    - `go test ./internal/bgp ./internal/dns -count=1` passes
    - No `hashmap` import references remain (hashmap package was only used by commented code)
  </acceptance_criteria>
  <action>
    1. In internal/bgp/main.go:
       a. Remove line 19: `//ipRefCounter *hashmap.Map[string, *atomic.Uint64]`
          This is the commented struct field declaration.
       b. Remove line 46: `//ipRefCounter: hashmap.New[string, *atomic.Uint64](),`
          This is the commented initialization in the Serve function.
       c. Verify no other hashmap imports exist — if the only hashmap reference was the commented code, no import cleanup needed.

    2. In internal/dns/resolvers.go:
       a. Remove the entire commented block at lines 127-148:
          ```go
          /*
              cause := e
              if unwrap, ok := cause.(interface{ Unwrap() error }); ok {
                  cause = unwrap.Unwrap()
              }
              ...
          */
          ```
          This is the 22-line commented error handling block after `rs.L().Error().Msg("All DNS Servers didn't respond")`.

    3. Verify: `go build ./internal/bgp ./internal/dns` succeeds.
    4. Verify: `go test ./internal/bgp ./internal/dns -count=1` passes.
  </action>
  <verify>
    <automated>go test ./internal/bgp ./internal/dns -count=1 2>&1</automated>
  </verify>
  <done>
    Commented hashmap removed from bgp/main.go (lines 19, 46). Commented error handling block removed from resolvers.go (lines 127-148). All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this is pure dead code removal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A for Go modules |
</threat_model>

<verification>
1. `go build ./internal/bgp ./internal/dns` succeeds
2. `go test ./internal/bgp ./internal/dns -count=1` passes
3. No commented hashmap references in bgp/main.go
4. No commented error handling block in resolvers.go
</verification>

<success_criteria>
- Commented hashmap line removed from bgp/main.go
- Commented hashmap initialization removed from bgp/main.go
- Commented error handling block removed from resolvers.go
- All tests pass after dead code removal
</success_criteria>

<output>
Create `.planning/phases/03-dependency-injection/03-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Type | File | Purpose |
|--------|------|------|---------|
| (none — dead code removal only) | — | — | No new symbols created |
