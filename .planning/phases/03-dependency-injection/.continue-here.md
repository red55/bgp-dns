---
context: phase
phase: 03-dependency-injection
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-06-16T06:10:36.772Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_None yet — Phase 3 has not started._

## Critical Anti-Patterns

_None recorded._

<current_state>
Phase 2 (Test Suite) is COMPLETE. All 3 waves executed and verified:
- 26 new test functions across 6 files
- All 63 tests pass (24 new + 39 existing)
- Race detector clean
- Build succeeds

Phase 3 (Dependency Injection) is PLANNED but NOT EXECUTED. All 6 plan files exist (03-01 through 03-06) across 6 waves. No plans have been executed yet.

No incomplete work, no blockers, no interrupted tasks. This is a clean transition point between phases.

Some plan files are not yet committed: 03-03-PLAN.md, 03-05-PLAN.md, 03-06-PLAN.md
</current_state>

<completed_work>

Phase 2 — All 3 waves complete:
- Wave 1 (02-01): BGP ref counting tests (6) + resolver failover tests (4)
- Wave 2 (02-02): Cache eviction tests (5) + gRPC CLI lifecycle tests (6)
- Wave 3 (02-03): E2E integration tests (3)

Phase 1 — All 3 plans complete (verified in previous session):
- 38 tests pass, 5/5 requirements verified

Phase 3 — Planning complete:
- 6 plans across 6 waves (15 tasks, 15 files)
- Wave 0: Foundation — typed configKey, Loop/logger injection, cache config, test updates
- Wave 1: DNS Service struct + constructors + wrappers
- Wave 2: BGP Service struct + constructors + wrappers
- Wave 3: FSWatcher Service struct + constructors + wrappers
- Wave 4: main.go error handling + startup sequence
- Wave 5: Dead code removal
- All plan-checker blockers resolved (3 initial blockers fixed)
</completed_work>

<remaining_work>

Phase 3: Dependency Injection — PLANNED, NOT EXECUTED
- Goal: Replace global package-level state with explicit dependency injection
- Requirements: REFACTOR-01, REFACTOR-02, REFACTOR-03, REFACTOR-04
- Dependencies: Phase 2 tests must pass before and after refactoring
- Success criteria:
  1. All subsystem constructors accept explicit dependencies
  2. Context config uses typed key instead of string "cfg"
  3. Startup failures return errors instead of panicking
  4. Dead code removed

Phase 4: Reliability — NOT STARTED
Phase 5: Security — NOT STARTED
</remaining_work>

<decisions_made>

- Phase 2 tests must work with existing global state (Phase 3 refactoring will improve testability)
- Phase 3 requires Phase 2 tests passing before and after refactoring (safety net)
- Phase 4 and Phase 5 can run in parallel after Phase 3 (per ROADMAP.md)
- Per-package constructors (NewDns, NewBgp, NewFsWatcher) — not central App struct
- Constructors return (Service, error) — no panics
- Keep thin wrapper functions for backward compatibility
- Typed configKey struct{} replaces string "cfg"
- Explicit logger parameter replaces log.L() calls
- Plan-checker blockers resolved: cfg field timing, loop parameter wiring, open questions marked RESOLVED
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. .planning/ROADMAP.md — Phase 3 goal, success criteria, dependencies
2. .planning/REQUIREMENTS.md — REFACTOR-01 through REFACTOR-04
3. .planning/PROJECT.md — Key Decisions table (DI decisions pending)
4. .planning/phases/02-test-suite/02-VERIFICATION.md — Phase 2 verification (tests to preserve)
5. .planning/phases/03-dependency-injection/03-CONTEXT.md — Locked decisions D-01 through D-09
6. .planning/phases/03-dependency-injection/03-RESEARCH.md — 13 globals identified, target code examples
7. .planning/phases/03-dependency-injection/03-PATTERNS.md — 10 files mapped to exact analogs
8. .planning/phases/03-dependency-injection/03-VALIDATION.md — Nyquist verification map
9. .planning/phases/03-dependency-injection/03-PLAN-CHECK.md — PASS (all issues resolved)
10. .planning/phases/03-dependency-injection/03-01-PLAN.md through 03-06-PLAN.md — All 6 plans
11. internal/dns/main.go — Current global state patterns in DNS package
12. internal/bgp/main.go — Current global state patterns in BGP package
13. internal/fswatcher/main.go — Current global state patterns in filesystem watcher
14. cmd/bgp-dnsd/main.go — Current startup/panic pattern

## Critical Anti-Patterns (do NOT repeat these)
- Do NOT remove global state before tests exist — Phase 2 provides the safety net
- Do NOT parallelize tests that touch shared globals — use sequential test execution
- Do NOT use interfaces for DI — use explicit constructor parameters (struct embedding)
- Ensure cfg field is added to fsWatcher struct in Wave 0 (not Wave 3) to avoid timing issues
- Pass loop parameter through to newCache — don't drop it in NewDns

## Infrastructure State
- No running servers or background processes
- All tests pass in clean state
- Some plan files not yet committed (03-03, 03-05, 03-06)

## Pre-Execution Critique Required
<!-- None — Phase 3 has not been planned yet -->

<context>
Project is in a clean state after completing Phase 2 and planning Phase 3. Phase 3 (Dependency Injection) is the next planned phase and depends on Phase 2 tests passing.

Key constraints for Phase 3:
- Must preserve behavioral equivalence (Phase 2 tests must pass before AND after)
- Replace package-level singletons with typed constructors
- Convert panic-on-init to error returns
- Use typed context keys instead of string "cfg"
- Remove dead code (commented error handling, commented hashmap)
- Keep thin wrapper functions for backward compatibility (Phase 2 tests pass unchanged)

The refactoring is significant — global state is deeply embedded in internal/dns, internal/bgp, internal/fswatcher, and cmd/bgp-dnsd packages. Phase 2 tests provide the regression safety net.

Phase 3 plans are ready. Wave 0 installs foundation (typed configKey, Loop/logger injection). Waves 1-3 create service structs in parallel. Wave 4 updates main.go. Wave 5 removes dead code.

Some plan files (03-03, 03-05, 03-06) are not yet committed — this is normal for planning phase.
</context>

<next_action>
Execute Phase 3: Dependency Injection.

Recommended first step: Run /gsd-execute-phase 3 to execute all 6 waves.

Phase 3 depends on Phase 2 tests passing before and after refactoring to verify behavioral equivalence.
</next_action>
